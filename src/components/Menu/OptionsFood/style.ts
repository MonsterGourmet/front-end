'use client'

import { styled } from "styled-components";

const SHorizontalNavBar = styled.nav`
     height: 60px;
     width: 100%;

     justify-content: center;
     flex-direction: row;
     align-items: center;
     display: flex;
     gap: 10px;

     overflow-x: auto;
`

export { SHorizontalNavBar }